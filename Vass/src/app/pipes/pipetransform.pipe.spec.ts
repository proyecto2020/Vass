import { PipetransformPipe } from './pipetransform.pipe';

describe('PipetransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PipetransformPipe();
    expect(pipe).toBeTruthy();
  });
});
